# Sesión 2026-05-02

## Estado al cerrar la sesión

### Completado y en master

- B1-B (CI básico en GitHub Actions) cerrada. Commits 1a34bf3, ed34185, 953f580, 18f6170, 6f987bc, 8d9ac95, 89bf390, 9846ea1.
- CLAUDE.md regla 9 añadida: sin Co-Authored-By en commits.
- BACKLOG actualizado con E12, E13.
- Docker Desktop instalado y verificado funcionando.

### En curso, NO cerrado

**B2 (migraciones SQL versionadas)** está en estado inconsistente.

Lo que se hizo:
- `supabase db pull` ejecutado, generó `supabase/migrations/20260502155455_remote_schema.sql` (32KB, 17 tablas, 49 RLS policies, 4 funciones trigger).
- Se respondió `y` al prompt "Update remote migration history table?", lo que registró la migración como `applied` en el historial remoto.
- Se inspeccionó el SQL parcialmente (cabecera, primeras 80 líneas, conteo de tablas y policies).
- Se creó `supabase/README.md` con instrucciones de aplicación (2.4KB).

Lo que se perdió/pasó después:
- El archivo `.sql` y el directorio `supabase/.temp/` desaparecieron del filesystem en algún momento entre la inspección y el intento de commit.
- El historial de PowerShell tenía huecos sospechosos (comandos ejecutados que no aparecían en `Get-History`).
- Diagnosticado: hay dos asistentes Claude operando sobre el mismo proyecto: este chat (Claude.ai) + Claude Code extension de Cursor. Probablemente la extensión hizo operaciones autónomas no rastreadas.

Decisión final: sesión cerrada sin completar B2. Pendiente:
1. Auditar estado real del repo en sesión nueva con cabeza fresca.
2. Regenerar el archivo `.sql` (cuidado: el remoto cree que `20260502155455` está aplicada).
3. Decidir el modelo de trabajo entre Claude.ai chat y Claude Code extension para este proyecto.

### Deuda pendiente identificada hoy

- **B2-DOC**: actualizar CLAUDE.md con las 7 tablas no documentadas (`conversations`, `conversation_members`, `messages`, `groups`, `group_members`, `group_posts`, `suggestions`) y las 4 funciones trigger (`handle_new_group`, `handle_new_message`, `handle_new_user`, `set_updated_at`).
- **B2-VERIFY**: verificar `supabase db reset` en local cuando sea posible.
- **E12, E13**: case-sensitivity hygiene (ya en BACKLOG).
- **E14**: reportar bug a Supabase CLI: `db dump --dry-run` imprime credenciales en stdout.
